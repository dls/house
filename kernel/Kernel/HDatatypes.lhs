-- THIS SHOULD PROBABLY GO AWAY

> module Kernel.HDatatypes(module H.All) where 

> import H.All(H,PAddr,PhysPage,VAddr,PageInfo(..),PageMap,
>              Context(..),Interrupt(..),ErrorCode,PageFaultErrorCode(..),IRQ(..))
